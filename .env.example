# Production-ready storage
# Set this to use PostgreSQL for lead storage.
# If omitted, app falls back to local file storage at data/leads.json
DATABASE_URL=

# Admin dashboard protection (Basic Auth) for /admin/* and /api/admin/*
# Required in production
ADMIN_USERNAME=
ADMIN_PASSWORD=
